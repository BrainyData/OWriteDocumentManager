;  overide this method to initialize the window     ;; called by $construct
;       ;; you must call the inherited method
Do inherited

Calculate ivUndoText as "Format Paragraph"

If ivIsForFind
	Calculate $cinst.$title as 'Find Paragraph'
End If