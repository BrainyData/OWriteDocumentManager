;  in order to reuse some code we simulate this button click as a document object click
;       ;; with the calculation set to call objOWriteEval.$showpdf
Quit method ivObjOWriteEval.$evalClickCalc($cinst(),0,'btnShowPDF','$cinst.$showpdf')