;  Enter your initialization code here
Calculate ivName as pName
Calculate ivGroup as pGroup
If not(pCanEditGroup)
	Calculate $cinst.$objs.Group.$enabled as kFalse
	Calculate $cinst.$objs.Group.$fieldstyle as 'CtrlEditTextDis'
End If
Enter data
If flag true
	Calculate pName as ivName
	Calculate pGroup as ivGroup
Else
	Calculate pName as ''
End If
Queue close [$cwind().$name]