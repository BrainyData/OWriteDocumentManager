If not(isnull(pValue))
	If not(len(pOutText))
		Calculate pOutText as con(pGroupText,' ')
	End If
	Calculate x as pValue
	While x
		Calculate txt as strtok('pValList',';')
		Calculate x as x-1
	End While
	Calculate pOutText as con(pOutText,txt)
End If