;  overide this method to load all the properties     ;; called by $construct and revert button
;       ;; also called by doNextPrev

Do method loadTable
Do method loadRow
Do method loadColumn
Do method loadCell
Do method setButtons

If pRedraw
	Do $cinst.$redraw()
	Do $cinst.$objs.Pages.$bobjs.1043.$redraw()
End If