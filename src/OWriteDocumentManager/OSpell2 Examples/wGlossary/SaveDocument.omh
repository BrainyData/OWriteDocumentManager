Begin reversible block
	Set read/write files {fData}
	Set main file {fData}
End reversible block
Find first
If flag true
	Prepare for edit
Else
	Prepare for insert
End If
Calculate fData.data as ivDocumentData
Update files
Calculate ivModified as kFalse